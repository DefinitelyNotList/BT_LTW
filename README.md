# Simple Blog React Project

Small React blog example with:

- public blog routes
- authentication with protected admin routes
- reusable components split into separate files
- create, edit, delete, and reset post actions
- localStorage persistence so it runs without a backend

## Run

```bash
npm install
npm run dev
```

## Demo Login

- Username: `admin`
- Password: `blog123`
